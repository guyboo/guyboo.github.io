// Re-export shared types for convenient client import.
export type {
  Pepper,
  InsertPepper,
} from "@shared/schema";

export {
  POT_SIZES,
  HEAT_LABELS,
  FLAVOR_GROUPS,
  FLAVOR_SUBTAGS_BY_GROUP,
  COLORS,
  SPECIES,
  AGES,
  STATUSES,
  PRIORITIES,
  POT_MATERIALS,
  USE_CASES,
} from "@shared/schema";
